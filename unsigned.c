#include <stdio.h>

int main(void)
{
    int a;
    char b;
    double c;
    printf("the sizoe of an int is: %lu", (unsigned long)sizeof(a));
    printf("The size of double is %lu \n", (unsigned long)sizeof(b));
    printf("The size of %lu", (unsigned long)sizeof(c));


}